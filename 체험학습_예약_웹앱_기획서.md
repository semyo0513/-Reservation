# 체험학습 프로그램 예약 웹앱 기획서

> **프로젝트명**: 체험학습 프로그램 선택 예약 시스템  
> **작성일**: 2025년 6월  
> **배포 환경**: GitHub Pages (프론트엔드) + Google Apps Script (백엔드/DB)

---

## 1. 프로젝트 개요

### 1.1 목적

학년별·반별 체험학습 프로그램 선택 신청을 온라인으로 처리하여 수기 접수의 번거로움을 없애고, 선착순 마감·오픈 시간 제어를 자동화한다.

### 1.2 대상 데이터

| 날짜 | 해당 반 | 프로그램 |
|------|---------|---------|
| 6월 30일 | 1, 4, 7반 | ① 전동 선풍기 조립 ② 조립 드론 만들기 ③ 미래 자동차와 코딩 ④ 디지털 굿즈 디자인 |
| 7월 1일 | 2, 5, 8반 | ① AI캐릭터 포토카드 디자인 체험 ② 버튼 제어형 Arduino 개발 실습 ③ 휴먼로봇 제작을 위한 코딩 ④ 3D프린팅 메이커 제작 체험 ⑤ 숏폼 제작을 위한 드론 활용 |
| 7월 2일 | 3, 6, 9반 | ① 공유압제어 체험 ② 미래 자동차를 만드는 3차원 측정 ③ MBTI 함수만들기 ④ 드론 비행 기초 입문 |

### 1.3 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | HTML / CSS / Vanilla JS (단일 파일) |
| 백엔드/DB | Google Apps Script (GAS) Web App |
| 데이터 저장 | Google Sheets |
| 호스팅 | GitHub Pages |
| 통신 방식 | `fetch()` → GAS `doGet` / `doPost` |

---

## 2. 시스템 구성도

```
[GitHub Pages - index.html]
        │  fetch (GET/POST)
        ▼
[GAS Web App URL]
        │  SpreadsheetApp
        ▼
[Google Sheets]
  ├─ programs  (프로그램 마스터)
  ├─ bookings  (신청 내역)
  ├─ settings  (전역 설정)
  └─ admins    (관리자 계정)
```

---

## 3. 화면 구성 및 기능 명세

### 3.1 학생 신청 화면 (메인)

**흐름**

```
학년·반·번호·이름 입력
        │
        ▼
[예매 오픈 여부 확인]  ── 미오픈 → "예매 시작 전입니다 (오픈 예정: OO시 OO분)" 표시
        │ 오픈됨
        ▼
해당 반 프로그램 목록 표시
  - 프로그램명
  - 정원 / 현재 신청 인원
  - 마감 여부 배지
        │
        ▼
프로그램 선택 (1개)
        │
        ▼
신청 확인 모달 → 제출
        │
        ▼
중복 신청 검사 → 성공 시 완료 메시지 / 실패 시 오류 메시지
```

**입력 필드**

| 필드 | 타입 | 검증 |
|------|------|------|
| 학년 | select (1~3) | 필수 |
| 반 | select (1~9) | 필수, 해당 반 프로그램 필터링 트리거 |
| 번호 | number | 1~40, 필수 |
| 이름 | text | 2~5자, 한글, 필수 |

**UX 규칙**

- 반 선택 시 해당 날짜·프로그램 자동 표시
- 정원 초과 프로그램은 선택 불가(회색 처리)
- 이미 신청한 경우 "신청 완료" 상태로 표시

---

### 3.2 관리자 화면

**접근**: 우측 하단 잠금 아이콘 → 비밀번호 입력 → 관리자 패널 진입

#### 3.2.1 대시보드

- 반별 신청 현황 요약 테이블
- 프로그램별 신청 인원 / 정원 프로그레스 바
- 전체 신청 목록 (학년·반·번호·이름·선택 프로그램·신청 시각)
- CSV 내보내기 버튼

#### 3.2.2 프로그램 관리

| 기능 | 설명 |
|------|------|
| 프로그램 추가 | 이름, 날짜, 대상 반(멀티셀렉), 정원 입력 |
| 프로그램 수정 | 이름·정원·대상 반 변경 |
| 프로그램 삭제 | 신청자 있을 경우 경고 후 삭제 |

#### 3.2.3 예매 오픈 시간 설정

- 날짜별(6/30, 7/1, 7/2) 오픈 일시 설정 (datetime-local)
- 즉시 오픈 / 즉시 마감 토글
- 설정 저장 시 GAS settings 시트에 반영

#### 3.2.4 신청 내역 관리

- 특정 학생 신청 취소(삭제) 기능
- 수동 신청 추가 기능 (현장 접수용)

---

## 4. Google Apps Script 설계

### 4.1 스프레드시트 구조

#### `programs` 시트

| id | name | date | targetClasses | capacity | createdAt |
|----|------|------|--------------|----------|-----------|
| p001 | 전동 선풍기 조립 | 2025-06-30 | 1,4,7 | 30 | ... |

#### `bookings` 시트

| id | grade | classNum | studentNum | studentName | programId | bookedAt |
|----|-------|----------|------------|-------------|-----------|---------|
| b001 | 1 | 4 | 12 | 홍길동 | p001 | 2025-06-28T09:00:00 |

#### `settings` 시트

| key | value |
|-----|-------|
| openTime_2025-06-30 | 2025-06-28T09:00:00 |
| openTime_2025-07-01 | 2025-06-28T09:00:00 |
| openTime_2025-07-02 | 2025-06-28T09:00:00 |
| adminPassword | (hashed) |

#### `admins` 시트 (선택)

| username | passwordHash |
|----------|-------------|
| admin | sha256(...) |

---

### 4.2 GAS API 엔드포인트 설계

모든 요청은 단일 `doGet` / `doPost` 진입점 사용 (기존 `dispatch()` 패턴 유지).

#### GET 액션

| action | 파라미터 | 응답 | 설명 |
|--------|---------|------|------|
| `getPrograms` | `classNum` | 프로그램 목록 + 현재 신청 수 | 학생 화면 초기 로드 |
| `checkOpenTime` | `date` | `{ isOpen, openTime }` | 오픈 여부 확인 |
| `getBookings` | `token` (관리자) | 전체 신청 목록 | 관리자 대시보드 |
| `getSettings` | `token` | 설정값 전체 | 관리자 설정 화면 |

#### POST 액션

| action | 파라미터 | 응답 | 설명 |
|--------|---------|------|------|
| `submitBooking` | `grade, classNum, studentNum, studentName, programId` | `{ success, message }` | 학생 신청 |
| `adminLogin` | `password` | `{ success, token }` | 관리자 로그인 |
| `saveProgram` | `token, program` | `{ success }` | 프로그램 추가/수정 |
| `deleteProgram` | `token, programId` | `{ success }` | 프로그램 삭제 |
| `saveSettings` | `token, settings` | `{ success }` | 오픈 시간 등 설정 저장 |
| `cancelBooking` | `token, bookingId` | `{ success }` | 신청 취소 (관리자) |
| `manualBooking` | `token, ...fields` | `{ success }` | 수동 신청 추가 |

---

### 4.3 핵심 GAS 로직

```javascript
// 선착순 마감 처리 (LockService 활용)
function submitBooking(params) {
  const lock = LockService.getScriptLock();
  lock.waitLock(10000);
  try {
    // 1. 오픈 시간 확인
    // 2. 중복 신청 확인
    // 3. 정원 확인
    // 4. 신청 등록
  } finally {
    lock.releaseLock();
  }
}
```

**중복 방지**: `grade + classNum + studentNum` 조합으로 이미 신청된 경우 차단  
**정원 초과 방지**: `LockService`로 동시 신청 레이스 컨디션 해결  
**관리자 인증**: 세션 토큰(UUID) 발급 → settings 시트에 만료 시각과 함께 저장

---

## 5. 프론트엔드 설계

### 5.1 파일 구조 (GitHub Repository)

```
repo-root/
├── index.html          ← 전체 앱 (HTML + CSS + JS 단일 파일)
├── README.md
└── (GAS 코드는 별도 Apps Script 프로젝트)
```

### 5.2 화면 상태 관리

```
STATE
├── view: 'student' | 'admin'
├── student
│   ├── grade, classNum, studentNum, studentName
│   ├── programs[]
│   └── selectedProgramId
└── admin
    ├── token
    ├── bookings[]
    ├── programs[]
    └── settings{}
```

### 5.3 UI/UX 요구사항

- 모바일 퍼스트 (학생들이 스마트폰으로 접속 상정)
- 오픈 전: 카운트다운 타이머 표시
- 정원 마감 프로그램: 빨간 배지 + 선택 비활성화
- 신청 완료 후 신청 내역 확인 화면 표시
- 관리자 패널: 별도 섹션으로 전환 (URL hash `#admin` 활용 가능)

---

## 6. 보안 고려사항

| 항목 | 처리 방법 |
|------|---------|
| 관리자 비밀번호 | SHA-256 해시 후 settings 시트 저장 |
| 관리자 세션 | UUID 토큰, 만료 시각(4시간) 설정 |
| 중복 신청 | 서버 사이드(GAS)에서 검증 (클라이언트 검증 불신) |
| CORS | GAS `doGet`/`doPost` 기본 허용, 추가 조치 불필요 |
| 정원 레이스 컨디션 | `LockService.getScriptLock()` |

---

## 7. 개발 순서 (추천)

```
Phase 1 - GAS 백엔드
  ├─ 스프레드시트 시트 구조 생성
  ├─ doGet / doPost dispatch() 뼈대
  ├─ getPrograms, checkOpenTime 구현
  ├─ submitBooking (LockService 포함)
  └─ 관리자 API (login, saveProgram, saveSettings, cancelBooking)

Phase 2 - 프론트엔드
  ├─ 학생 신청 화면 (입력 → 프로그램 목록 → 선택 → 완료)
  ├─ 오픈 시간 전 카운트다운 화면
  └─ 관리자 패널 (대시보드 → 설정 → 프로그램 관리)

Phase 3 - GitHub Pages 배포
  ├─ 저장소 생성 및 index.html 푸시
  ├─ Settings > Pages > main branch 배포
  └─ GAS URL index.html에 상수로 삽입

Phase 4 - 테스트
  ├─ 동시 신청 부하 테스트 (LockService 검증)
  ├─ 오픈 시간 전후 동작 확인
  └─ 모바일 브라우저 렌더링 확인
```

---

## 8. 추가 고려 기능 (선택)

| 기능 | 우선순위 | 비고 |
|------|---------|------|
| 신청 확인 화면 (학번으로 조회) | ★★★ | 학생 재확인용 |
| 관리자 CSV 다운로드 | ★★★ | 명단 출력용 |
| 신청 대기 순번 표시 | ★★☆ | UX 개선 |
| 프로그램별 신청자 명단 인쇄 | ★★☆ | 현장 운영용 |
| 카카오톡 알림 연동 | ★☆☆ | 추후 확장 |

---

## 9. 산출물 목록

| 산출물 | 형태 | 비고 |
|--------|------|------|
| `index.html` | 단일 HTML 파일 | GitHub Pages 배포 |
| `gas_code.gs` | GAS 스크립트 | Apps Script 프로젝트에 붙여넣기 |
| Google Sheets 템플릿 | 스프레드시트 | programs / bookings / settings 시트 포함 |

---

*본 기획서는 실제 개발 착수 전 검토·수정하여 활용하세요.*
